[project]
name = "max-studio-gateway"
version = "0.1.0"
description = "API Gateway FastAPI para MAX Studio"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py312"
